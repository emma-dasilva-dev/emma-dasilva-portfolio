import type { ContactFieldErrors, ContactFormPayload } from "@/types/portfolio";
export const CONTACT_LIMITS={email:254,subjectMin:3,subjectMax:120,messageMin:10,messageMax:2000,minCompletionMs:1200} as const;
export function normalizeContactPayload(value:unknown):ContactFormPayload|null{ if(!value||typeof value!=="object")return null; const v=value as Record<string,unknown>; if(typeof v.email!=="string"||typeof v.subject!=="string"||typeof v.message!=="string"||typeof v.startedAt!=="number")return null; return {email:v.email.trim(),subject:v.subject.trim(),message:v.message.trim(),company:typeof v.company==="string"?v.company.trim():"",startedAt:v.startedAt}; }
export function validateContactPayload(p:ContactFormPayload):ContactFieldErrors{ const e:ContactFieldErrors={}; if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(p.email)||p.email.length>CONTACT_LIMITS.email)e.email="invalid_email"; if(p.subject.length<CONTACT_LIMITS.subjectMin||p.subject.length>CONTACT_LIMITS.subjectMax)e.subject="invalid_subject"; if(p.message.length<CONTACT_LIMITS.messageMin||p.message.length>CONTACT_LIMITS.messageMax)e.message="invalid_message"; if(p.company||(Date.now()-p.startedAt)<CONTACT_LIMITS.minCompletionMs)e.form="spam_detected"; return e; }
export const hasContactErrors=(e:ContactFieldErrors)=>Object.keys(e).length>0;
export const escapeHtml=(s:string)=>s.replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]??c));
