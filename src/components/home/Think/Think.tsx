"use client";
import {useLocale} from "@/components/providers/LocaleProvider/LocaleProvider";import {HOME_COPY,PRINCIPLES} from "@/content/home";import styles from "./Think.module.css";export default function Think(){const{locale}=useLocale();const c=HOME_COPY[locale].think;return <section id="think" className="section"><div className="shell"><div className={styles.header}><h2 className="sectionHeading">{c.label}</h2><p className="sectionIntro">{c.introduction}</p></div><div className={styles.grid}>{PRINCIPLES.map(p=><article key={p.id}><h3>{p.title[locale]}</h3><p>{p.description[locale]}</p></article>)}</div></div></section>}
