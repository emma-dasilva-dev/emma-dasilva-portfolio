"use client";
import Image from "next/image";import {useLocale} from "@/components/providers/LocaleProvider/LocaleProvider";import {HOME_COPY} from "@/content/home";import styles from "./Who.module.css";export default function Who(){const{locale}=useLocale();const c=HOME_COPY[locale].who;return <section id="who" className="section"><div className={`shell ${styles.grid}`}><div><h2 className="sectionHeading">{c.label}</h2><div className={styles.copy}>{c.paragraphs.map(p=><p key={p}>{p}</p>)}</div></div><div className={styles.portrait}><Image src="/images/portrait/emma-portrait.jpg" alt={c.portraitAlt} fill sizes="(max-width:768px) 92vw,34vw"/></div></div></section>}
