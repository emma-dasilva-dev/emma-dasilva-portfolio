"use client";
import {useLocale} from "@/components/providers/LocaleProvider/LocaleProvider";import {HOME_COPY} from "@/content/home";import {PROJECTS} from "@/content/projects";import styles from "./Build.module.css";export default function Build(){const{locale}=useLocale();const c=HOME_COPY[locale].build;return <section id="build" className="section"><div className="shell"><div className={styles.header}><h2 className="sectionHeading">{c.label}</h2><p className="sectionIntro">{c.introduction}</p></div><div className={styles.list}>{PROJECTS.map(p=><article key={p.id} className={styles.project}><div><p className={styles.category}>{p.category[locale]}</p><h3>{p.title}</h3></div><div className={styles.details}><p>{p.description[locale]}</p><p>{p.context[locale]}</p><ul>{p.technologies.map(t=><li key={t}>{t}</li>)}</ul>{p.liveUrl&&p.action?<a href={p.liveUrl} target="_blank" rel="noreferrer">{p.action[locale]} ↗</a>:<span>{p.status?.[locale]}</span>}</div></article>)}</div></div></section>}
